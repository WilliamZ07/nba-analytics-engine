name: nba_lakehouse
version: 1.0.0
config-version: 2

profile: nba_lakehouse

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  nba_lakehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table